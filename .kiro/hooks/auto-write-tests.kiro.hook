{
  "enabled": true,
  "name": "Auto Write Tests",
  "description": "Agent 执行结束后，检查本次修改的代码文件并自动编写或更新对应的单元测试用例",
  "version": "1",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "askAgent",
    "prompt": "回顾本次修改的所有代码文件（排除测试文件本身和 .md/.json 等非代码文件）。对于每个修改过的代码文件，检查同目录下是否有对应的测试文件。如果有，为本次新增或修改的功能补充测试用例；如果没有，创建新的测试文件。遵循项目现有的测试风格和命名规范（Go 项目用 _test.go，放在同包下）。如果本次没有修改任何代码文件，跳过。"
  }
}